# ClassifyOS backend dependencies.
# Loose lower bounds here; exact versions are pinned via `pip freeze` after the
# first install (governance: reproducible env / hallucination check).
# Target runtime: Python 3.11.

# --- ML engine ---
numpy>=1.26,<3
pandas>=2.1,<3
scikit-learn>=1.4,<2
imbalanced-learn>=0.12,<1     # SMOTE / class imbalance (Section 8)
xgboost>=2.0,<4              # XGBoostModel wrapper (Section 11)
lightgbm>=4.0,<5             # LightGBMModel wrapper (Section 11)
matplotlib>=3.8,<4            # plot_results (Section 14)
optuna>=3.6,<5               # hyperparameter tuning (Section 8B)
joblib>=1.3                   # model persistence
openpyxl>=3.1                 # .xlsx reads in data_loader (Section 4)
pyarrow>=15.0                 # .parquet reads in data_loader (Section 4)

# --- API layer ---
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
pydantic>=2.6,<3              # RunConfig (v2 validators)
pydantic-settings>=2.2,<3     # env-backed settings
python-multipart>=0.0.9       # file upload endpoints
python-dotenv>=1.0            # load backend/.env

# --- tests ---
pytest>=8.0,<9
httpx>=0.27                   # FastAPI TestClient transport
