# ClassifyOS — Plan Deviation & Assumption Register

> Honest record of every place the build deviated from the signed scope document / 4-week
> plan, or made an assumption the plan didn't cover. A register, not an essay — one line
> per cell. "Impact" = what a reviewer (Naveen / stakeholders) needs to know at sign-off.
>
> Covers Phases 0–9. Updated at the end of every phase.

| # | Phase | What the plan said | What we did instead / assumed | Why | Impact |
|---|---|---|---|---|---|
| 1 | 0 | Single `classification_framework.py` holding all 16 sections | Split into modules/packages under `backend/classifyos/` | Maintainability; module boundaries enforce the "additive sections" rule; easier for GenAI iteration | Structural only; section-to-file map is in CLAUDE.md. No behaviour change |
| 2 | 0 | Single-file HTML dashboard (`classify_ui.html`) | React (Vite + TypeScript) frontend, 13 pages | 13 pages too large for one file; intended for future Sapiens website integration | Scope's UI deliverable is superseded; frontend not yet built (Phase 9) |
| 3 | 0 | No storage abstraction in scope | Added `StorageAdapter` + env-configured `DATA_DIR`/`OUTPUT_DIR` | Clean local→Databricks (Unity Catalog volumes) swap later; no hardcoded paths | New abstraction reviewers should know about; all I/O routes through it |
| 4 | 1/3 | Step order: preprocess = step 3, split = step 6 | Split runs BEFORE preprocessing | Scope's own leakage rule ("scaler fitted on train only") contradicts its step order | **Scope doc §4 order diagram is outdated** — canonical order is in PROJECT_STATE decisions log |
| 5 | 1 | Real insurance CSVs to validate against | Synthetic datasets with constructed signal (`generate_sample_data.py`) | Real data unavailable at build time | **Metrics on synthetic data are not representative; real-data revalidation pending** |
| 6 | 1/3 | Fixed RunConfig key table | Added `random_state`, `time_split_col`, `outlier_method`, `high_cardinality_threshold` | Reproducibility, temporal splits, and Section 6 tunables not covered by scope | Config contract is wider than scope's table; documented in DEFAULT_CONFIG |
| 7 | 3 | A plain `preprocess()` function | `Preprocessor` as a picklable fit/transform class | Needed for train-only fitting and later `/api/explain` reuse | API shape differs from scope; behaviour matches intent |
| 8 | 2/3 | Scope addressed binary framing | Multiclass adaptations: correlation ratio (eta) replaces point-biserial; frequency encoding replaces target encoding for high-cardinality multiclass | Point-biserial and target-mean encoding are ill-defined for 3+ classes | Methodology choice reviewers should confirm is acceptable for multiclass use cases |
| 9 | 2 | (Not specified) | Test outputs isolated to pytest temp dirs, not real `OUTPUT_DIR` | Tests must never pollute real output artifacts | Test-hygiene assumption; no production impact |
| 10 | 1/2 | Output schema contract locks at Phase 8 | `inspect_file()` return keys + feature_impact output columns locked early | Frontend/contract stability needed before later code depends on them | Early lock; reviewers should treat these keys as fixed ahead of the Phase 8 lock |
| 11 | 2 | (Not in scope) | `id_like` flag for ≥99%-unique columns in feature impact | Leakage guard — identifier columns flagged, not silently dropped | Extra safety signal; columns flagged not removed (human decides) |
| 12 | 4 | Plain `build_features()` / `build_interaction_features()` functions | `FeatureBuilder` + `InteractionFeatureBuilder` picklable fit/transform classes | Train-only fitting (poly ranking, ratio denominator, bin edges, MI auto-discovery) requires a fit/transform split, like the Preprocessor | API shape differs from scope; behaviour matches intent. The scope's plain functions leaked via in-place frequency encoding / per-call binning / re-discovery |
| 13 | 4 | Frequency encoding listed in BOTH Section 6 and Section 7 | Frequency / high-cardinality encoding lives ONLY in Phase 3's Preprocessor (Section 6); Section 7 does not re-encode | Double-encoding is wrong and ambiguous about which fit wins | Section 7 builds poly/ratio/bin features only; no categorical encoding |
| 14 | 4 | Polynomial features (degree 2) as a standard capability | Polynomial defaults **OFF** and is capped at `max_poly_features` (ranked by |train corr| with target) when on | Squared terms are usually redundant with tree models and explode width/collinearity | Reviewers should opt in explicitly; the cap bounds the column count |
| 15 | 4 | Auto-discover interaction pairs by MI gain | Candidate pool capped at the 15 most target-correlated numeric columns before pairing; scoring uses the multiplicative term; kept pairs materialized with `default_interactions` ops | O(n²) pair explosion otherwise; "interaction_term" is singular in the scope | Discovery considers ≤105 pairs, not all C(n,2); a strong pair outside the top-15 pool can be missed |
| 16 | 4 | Ratio denominator unspecified vs scaling | FeatureBuilder's heuristic ratio denominator (largest |train median|) is weakly determined post-standard-scaling (medians ≈ 0) and often small → guard fires → 0.0 | Pipeline runs features AFTER preprocessing (incl. scaling) | Heuristic ratios are noisy by default; explicit interaction_pairs (Section 7B) are the reliable path. Per-row guard prevents inf |
| 17 | 4 | Binning fires on skewed numerics (|skew|>1.5) | With default IQR outlier capping the lognormal tail is clipped (skew drops below 1.5), so binning rarely fires unless `outlier_method="none"` | Capping precedes feature engineering in the pipeline | Binning is conditional on the capping setting; reviewers should know the two interact |
| 18 | 5 | SMOTE with a fixed default `k_neighbors` (5) | `k_neighbors` is auto-reduced to `min(5, minority_count-1)` when the minority is small, and a minority of ≤1 sample falls back to `RandomOverSampler` (duplication) with a logged warning | SMOTE errors when `k_neighbors >= minority_count` and cannot interpolate from a single point; fraud (~99:1) routinely produces tiny minorities | Auto-guard keeps the pipeline robust on extreme ratios; reviewers should note the ≤1-sample fallback adds no synthetic variety (pure duplication) |
| 19 | 5 | Class balancing applies to all problem types | Multilabel targets cannot be SMOTE'd/undersampled; the balancer falls back to `class_weight` with a logged warning | A multilabel row carries several labels at once, so there is no single class for imbalanced-learn's 1-D samplers to balance on | **Multilabel resampling is unsupported in v1.0** — multilabel imbalance is handled only via class weights |
| 20 | 6 | "Some models take the `class_weight` dict directly, some need sample_weight translation" | ALL six wrappers consume `class_weight` uniformly by translating the `{class: weight}` dict to a per-sample `sample_weight` vector at fit time | The loader coerces targets to string dtype, so numeric labels arrive as `"0"/"1"`; sklearn's native `class_weight`-dict path int-coerces them and then can't find the string keys (`ValueError: classes [0,1] are not in class_weight`). Per-sample weights are mathematically equivalent and library-agnostic | Behaviour matches intent (equivalent training); the difference is purely how the weight is plumbed. No native `class_weight` arg is passed to any estimator |
| 21 | 6 | `SVMModel` via `SVC(probability=True)` OR `CalibratedClassifierCV` | Used `CalibratedClassifierCV(SVC(), ensemble=False)` | `SVC(probability=True)` is deprecated in scikit-learn 1.9 and removed in 1.11; the calibrated wrapper is the sanctioned replacement | SVM `feature_importance` is always `None` (the calibrated wrapper exposes no `coef_`); correct for the default RBF kernel regardless. SVM is the slow model (internal CV calibration) |
| 22 | 6 | xgboost/lightgbm assumed available; XGBoost handles labels | xgboost (3.2.0) + lightgbm (4.6.0) were not installed/listed — installed + added to `requirements.txt`; `requirements.lock` pinned via `pip freeze`. XGBoost wrapper label-encodes `y` to `0..n-1` internally | The two boosting wrappers require these libs; `XGBClassifier` 3.2.0 rejects string/non-consecutive labels, and the engine's targets are strings | Two new runtime deps reviewers should note; the lock file is the reproducible-env record. XGBoost predictions are mapped back to the original string labels, so the wrapper contract is unchanged |
| 23 | 7 | `plot_results` produces all 6 plots; "ROC + PR per class/algorithm" | plots.py writes plot1/2/3/5 ONLY (plot4 feature-impact and plot6 interactions are written upstream in Sections 5/7B and not duplicated). For **multiclass**, plot2 is one-vs-rest ROC per class (PR omitted as ill-defined) and plot5 calibration is binary-only; both, plus plot3 when no model exposes importances, fall back to a **labelled placeholder PNG** rather than a missing file | The scope assigned all 6 plots to Section 14, but Sections 5/7B already own plot4/plot6; duplicating them would risk drift. Multiclass PR/calibration are genuinely ill-defined in the binary form | The OUTPUT_DIR artifact set is always complete (frontend can rely on every plot key existing); reviewers should know plot5/plot3 may be a placeholder for some problem types / model mixes, and multiclass plot2 shows ROC only |
| — | 7 | (corrected pipeline order — already recorded) | ModelRunner implements the corrected canonical order (split BEFORE preprocessing); **no new deviation** — it realises the Phase 3 decision (row 4 above). The `_run_config` deep-copy isolation and robust per-algorithm failure handling are scope requirements, faithfully implemented (not deviations) | — | Cross-reference only: the runner is the first place the full corrected order runs as one call |
| 24 | 7B | **AutoML / hyperparameter search was OUT OF SCOPE for v1.0** (scope doc deferred it to v1.5) | Built an Optuna tuning layer (`tuning.py`) in v1.0 — OFF by default; Optuna/TPE over grid search; k-fold CV-in-train as the default trial scoring (single train-internal split optional); one uniform mechanism for all six models with rich spaces for the tree/linear models and honestly-minimal spaces for SVM/NaiveBayes | Sanctioned deviation requested this session; Optuna gives smarter search than a grid for the same budget, and CV-in-train keeps trial scoring leakage-free | **A scope feature was pulled forward from v1.5.** Tuning is opt-in (`tuning.enabled`), wraps the Phase 6 wrappers without modifying them, and is isolated per-model (a failed study → defaults, never aborts the run). Reviewers should note SVM/NaiveBayes search spaces are deliberately thin |
| 25 | 7B | The tuning prompt specified `timeout_seconds: None` (no cap; only the trial count bounds a study) | Default changed to a **hard `600`-second per-model cap** (explicit `None` still accepted as an opt-out) | With `models=[]` (tune-all) and `n_trials=30`, an unbounded default would run a 30-trial study for every algorithm including the slow calibrated-SVM — an open-ended run. A finite default ceiling makes a tuning run impossible to leave running unbounded | Behaviour differs from the prompt's literal default; a study now stops at the timeout OR the trial cap, whichever first. Also: best params are read from the best trial's `user_attrs` (not `study.best_params`) so transformed suggestions round-trip correctly; balancing is applied only to the final fit, not inside CV folds (the prompt's documented safe default) |
| 26 | 7B | The tuning prompt specified the LogisticRegression space as "C (log) + penalty/solver compatible pairs" | LR tunes **`C` only**; solver/penalty are left at the wrapper defaults (lbfgs + L2) | Verified against installed scikit-learn 1.9: the `penalty` arg is **deprecated** (warns since 1.8, removal in 1.10) and the `liblinear` solver **rejects multiclass** (`n_classes >= 3`), so the prompt's pairs raised `FutureWarning`s on every fit and hard-errored on multiclass targets. The non-deprecated way to tune the penalty *type* needs `solver="saga"` + `l1_ratio` (slow; risks non-convergence at our fixed `max_iter`) — deferred | Found when a user tuned LR on the 3-class iris set (liblinear trials failed; penalty FutureWarnings). LR's tuning is now thinner than the prompt envisaged but warning-free and binary/multiclass-uniform; `C` is LR's dominant knob. Fixed post-Phase-7B-commit |
| 27 | 8 | Engine frozen except the sanctioned curve helper | Added `evaluation/curves.py` (`compute_curve_points`) as a NEW additive module and refactored `plot_results` plot2 to draw from it | The frontend's Chart.js needs ROC/PR *point arrays*, which plot2 already derived; one shared helper stops the PNG and the JSON curves from drifting | **Sanctioned engine edit during the API phase.** Additive (new module); plot2's filename/appearance/placeholder behaviour unchanged — only the source of its points. The helper reads held-out test predictions only and fits nothing (leakage-safe by construction) |
| 28 | 8 | (Scope listed `/run` as an endpoint without an execution model) | `POST /api/v1/run` is **synchronous**: it runs the pipeline on a worker thread (`run_in_threadpool`) and returns the full result in one response | The engine is synchronous CPU-heavy Python; a threadpool keeps the event loop responsive without a job queue. A submit→poll→fetch background path is real extra infrastructure | **A long run can exceed a reverse-proxy/gateway timeout.** v1.0 accepts this; background jobs are **deferred to v1.5**. Documented in the `/run` docstring and `docs/api_contract.md` |
| 29 | 8 | `/explain` (single-row SHAP) listed without addressing statelessness | Shipped option **(B)**: a structured "explainability requires a persisted model (v2.0)" stub for **all** models; no training on request | A FastAPI process holds no model between requests and v1.0 has no model registry; `shap` is not installed. The prompt's default (A) (re-fit + `TreeExplainer` for tree models) needs a heavy dep + full retraining per call | **No real SHAP in v1.0.** The response *shape* (`method`/`shap_values`/`base_value` = null) is final so v2.0 (model persistence / MLflow) fills it in without a contract change. Owner-confirmed choice |
| 30 | 8 | Scope doc's endpoint table used a bare `/api/...` prefix | All routes mounted under **`/api/v1/`** | CLAUDE.md mandates the `/api/v1/` prefix (gateway/SSO/versioning readiness); the scope table predates it | Route prefix `/api/v1/` **supersedes** the scope doc's `/api/...` table. No behavioural impact; the frontend targets `/api/v1/` |
| 31 | 8 | "StorageAdapter for ALL file I/O" + "no other engine edits" | Added an **additive** `save_input(key, fileobj)` method to the `StorageAdapter` ABC + `LocalFolderStorage` (writes into the INPUT root, traversal-guarded) | `open_write` targets `OUTPUT_DIR`, but `inspect_file`/`data_loader` read from `DATA_DIR`, so an uploaded file saved via the existing API could not be read back by `/run`. The only fully rule-compliant fix is an adapter method | **A second sanctioned engine edit beyond the curve helper**, made necessary by the `/upload` requirement. Honors CLAUDE.md's higher-priority "ALL I/O through StorageAdapter" NEVER-rule over the phase prompt's "no other engine edits". Additive, no existing behaviour changed. Owner-confirmed |
| 32 | 9a | "Uses shadcn/ui for components" | Button/Card/Badge/Input/Label are genuine shadcn/ui (CVA + Tailwind tokens); **Select and Switch are accessible native HTML elements** styled to match, NOT the Radix-based shadcn versions | The shadcn Select/Switch pull in `@radix-ui/*`; native `<select>`/`<input type=checkbox>` are fully keyboard/screen-reader accessible, dependency-free, and the clearest thing for a frontend newcomer to read. The token theming (the part the design pick is about) is identical | **Two of the shadcn components are native-element equivalents in 9a.** Drop-in upgradeable to the Radix shadcn components later if a page needs custom option rendering. No contract or behavioural impact. `PROJECT_WISDOM.md` (named in the 9a prompt's read-list) **does not exist** in the repo — the `.env`/CORS rules it referenced are in CLAUDE.md + `docs/api_contract.md`, which were read instead. **No `/api/v1/run` contract gaps were found** — every UI field maps to a contract field |
| 33 | 9c | Scope listed **13 pages / a 14-item-style nav** with Overview and Pipeline as separate screens | **Overview and Pipeline were merged into one page** (the run lives where its summary lives); the separate Pipeline page was deleted and `/pipeline` redirects to `/`. Final count: **12 nav items / 12 pages.** Also: the merged "while running" view shows the canonical pipeline stages as a static checklist + spinner rather than a "live log" | `/run` is synchronous and returns everything in one response — there is no incremental log to stream, and Overview + Pipeline are the two ends of one Configure→Run→watch→results flow. One continuous screen matches the mental model; the redirect keeps existing links/state working | **Final page/nav count is 12, not the scope's 13.** Pure UX consolidation — no contract or behavioural impact, no pages dropped (Pipeline's content is now Overview's in-progress + scoreboard/artifacts/raw-envelope states). The "live log" the prompt mentioned does not exist because the API does not stream; the honest stage checklist is shown instead |
| 34 | 11 | Multilabel listed as a supported problem type | **Wired multilabel end-to-end for the first time.** A multilabel target is one `\|`-delimited column (e.g. `Auto\|Home`); a new additive module `classifyos/multilabel.py` + a `MultiLabelBinarizer` (fitted on TRAIN only) build the indicator matrix the OvR wrappers + `evaluate_model` already expected. Per-label metrics / one-vs-rest ROC-PR curves / per-label class report / label-set predictions all populate; the single confusion matrix, MCC and log-loss are `None` (undefined for a multi-hot target) | The leaf-level multilabel branches existed (wrappers' OvR, `_evaluate_multilabel`) but NOTHING in the orchestrator built the indicator matrix, so a multilabel run silently degenerated into "multiclass over the 63 delimited combos". The phase sanctioned multilabel-only engine edits to fix this | **Additive, multilabel-keyed** edits to runner/predict/curves/plots/api + the new module; **binary & multiclass are byte-for-byte unchanged** (the 184 pre-existing tests stay green). The locked contract did NOT change — multilabel renders through the existing envelope. Regression tests: `tests/test_multilabel.py` (+ the multilabel row in `test_api_run.py` and the 7-use-case sweep). [RISK] notes added (MLB train-only; `\|` delimiter convention) |
| 35 | 11 | "Make multilabel work" (scope boundary) | **Scope conclusion:** multilabel is shipped as "runs end-to-end + renders honestly WITH its documented limitations", NOT full feature parity. Per-label thresholds stay out of scope (v1.x); multilabel **imbalance is effectively unhandled** in v1.0 — resampling is N/A (the documented `smote`→`class_weight` fallback fires) and the OvR wrapper does not apply the computed `class_weight`; a single confusion matrix is omitted by design | §2 of the phase prompt: do not expand scope on the final day — a documented honest limitation beats a rushed half-feature | **Reviewers:** multilabel is v1.0-usable for per-label scoring / ranking, but per-label thresholding and imbalance weighting are explicit v1.x items. This is the surfaced judgment call the phase asked for |
| 36 | 11 | "Keep [new sample data] out of git like the other data" | The **4 new use-case CSVs** (claim_likelihood, customer_segment, claim_severity, product_reco) ARE committed to `backend/data/samples/`, like the existing 3; only the **12k perf set** (`perf_lapse_12k.csv`) is kept out of git (DATA_DIR only) | The prompt assumed sample data is gitignored, but in THIS repo the samples are the **committed portable E2E seed** (the existing 3 are tracked). The 7-use-case browser E2E must be reproducible on a fresh clone / CI, which requires the CSVs to travel | Deliberate, documented deviation from the prompt's wording in favour of the repo's actual convention + E2E reproducibility. The large perf benchmark set is excluded (throwaway artifact, regeneratable via `--perf`) |
| 37 | 11 | Scope target: a run completes in "< 5 min on a standard laptop" | **Measured: 12,000 rows × 4 algorithms (LR/RF/XGB/LGBM), tuning OFF = 13.0s** — comfortably within target. Tuning sanity (XGBoost, 25 trials, cv=3) = 65.7s, bounded by `n_trials` before the 600s per-model ceiling | Phase 11 performance-baseline workstream (synthetic data; real-insurance revalidation still pending — row 5) | **No deviation — the target is met with large headroom.** The synchronous-`/run` gateway-timeout risk (row 28) does not bite at this size (13s); it remains a v1.5 background-job concern only for much larger data or tuning-on |
| 38 | 7B.2 | (Phase 7B's tuning search spaces, as originally built) | Expanded three Optuna search spaces from `docs/tuning_audit.md`: LightGBM `max_depth` (3–12, bounds leaf-wise growth vs the unbounded default), XGBoost `gamma`/min_split_loss (0–5), SVM `kernel` made a real `["rbf","linear"]` categorical with **conditional** `gamma` (rbf-only) | Audit-flagged gaps (LGBM depth↔leaves overfitting risk; gamma a distinct regulariser; the SVM `["rbf"]` categorical was a no-op). Lower-priority audit items (min_split_gain, max_delta_step, RF depth ceiling, LR penalty/solver) deliberately deferred | **Refinement of the already-sanctioned Phase 7B tuning layer (row 24), NOT a new scope deviation.** A non-tuning run is byte-for-byte unchanged; tuning stays OFF by default. Engine-only, additive to the `_space_*` functions; verified vs installed lightgbm 4.6.0 / xgboost 3.2.0 / sklearn 1.9.0. +4 tests (206 total) |
| 39 | post-11 | The `/api/v1/run` response schema was LOCKED at `1.0` (Phase 8) | **First version bump of the locked contract: `1.0` → `1.1`, done additively.** Added one new OPTIONAL `result.tuning` block (the per-model tuned hyperparameters the engine already produces in `run_profile.json`) and bumped `RunResponse.schema_version` default to `"1.1"`. No existing `1.0` field was renamed, retyped, or removed | The tuned hyperparameters stopped at the API response boundary (engine produced them, the locked envelope omitted them — see `docs/tuned_params_path_audit.md` Option 1). The dashboard needs a typed, versioned field rather than scraping the `run_profile.json` artifact | **Additive, API-only — zero engine change** (the runner already holds `tuned_params_` and writes the `tuning` block; the serializer just copies it). `result.tuning` is `null` when tuning was OFF, so a non-tuning run is byte-identical to `1.0`. Safe because the frontend parser is version-tolerant (checks `schema_version` is a string, validates only known keys) — the UI is updated in a separate session. Existing `/run` tests updated to assert `"1.1"` + the new key; +2 tuning tests. `/explain` keeps its own `1.0` envelope (separate endpoint, out of scope) |
| 40 | 14 | (Not in scope) — feature engineering was a fixed set of automatic transforms (poly/ratio/bin/interactions) | **New capability: USER-DEFINED structured features** (`UserFeatureBuilder`). A user specifies new columns as STRUCTURED specs — `[col_a] + [op from a fixed allowlist] + [col_b]` (numeric `add/subtract/multiply/divide/ratio`; `datetime_diff` subtract → duration in a chosen unit) or single-column transforms (`log/abs/bin`, date-parts `year/month/day/dayofweek/hour`). **Explicitly NOT free-text formulas** — nothing is ever `eval`/`exec`'d; only known ops on known columns ([RISK]-marked). Sanctioned config addition (`user_features: []`, validated as an allowlist guard) + sanctioned ModelRunner edit; new leakage-safe fit/transform module | Requested this session. Lets analysts add domain features (e.g. `duration = end_time − start_time`, `premium_per_sum`) without code, with the no-eval safety rule and the same train-only fit/transform discipline as FeatureBuilder | **Additive; OFF by default** (`user_features=[]` → a run is byte-for-byte unchanged, asserted by a test). Engine-only this session (API/UI are separate follow-ups). +24 tests. **Design call:** the builder is positioned after preprocessing + FeatureBuilder and before interactions (as the prompt specified), but it READS its source columns from the RAW post-split frame, not the preprocessed one — required because the Preprocessor scales numerics and encodes/drops datetime columns, so `datetime_diff` and meaningful numeric ops are impossible post-preprocessing. Output columns are NaN-free (numeric→0.0, coded→−1) so they feed balancing/training safely. Invalid specs are skipped+logged (run never aborts); the config boundary hard-rejects unknown ops/types |
| 41 | post-14 | (Engine-only in Phase 14) — `user_features` existed only on the engine config; the API request could not carry it | **Exposed `user_features` on the `/api/v1/run` REQUEST** (additive, request-side only). New `UserFeatureSpec` Pydantic sub-model + optional `RunConfig.user_features` list (default `[]`); a fast-fail allowlist check (unknown `type`/`op`, or a two-column type missing `col_b` → 422) mirrors the engine's `USER_FEATURE_*` constants (imported, not duplicated); `to_engine_config` dumps each spec with `exclude_none` and forwards it to `build_config` (the authoritative validator). **No response-schema change and NO version bump** — the created columns are real engineered columns that already surface in `result.run.active_features` (verified end-to-end) | The dashboard needs to send dashboard-defined feature specs; the engine already accepts them, so only the request boundary needed widening | **Additive, API-only; response stays at `1.1`.** A request with no `user_features` is byte-identical to before (asserted by a test). The `exclude_none` dump is required because the engine treats a present `unit=None`/`col_b=None` as invalid. +5 tests (a valid numeric-divide + date-part run shows the columns in `active_features`; unknown-op and missing-`col_b` → 422; two mapping unit tests). Note: the sample CSVs have only one datetime column, so the two-column `datetime_diff` path is documented but not E2E-tested here (covered by the engine's own tests). UI follow-up is a separate session |
| 42 | post-16 | Interaction features (Section 7B) are a standard, configurable pipeline stage | **TEMPORARILY UNWIRED by request (reversible, NOT deleted).** `ModelRunner._engineer` force-disables `interaction_features` on the deep-copied run config regardless of the request, so `InteractionFeatureBuilder` short-circuits and `plot6` is not written; `result.run.interaction_cols` returns `[]`. UI hidden: the Configure card, the sidebar nav entry, and the `/interactions` route+import are commented out (`/interactions`→`/` redirect); `Interactions.tsx`/`results.ts` kept intact (unreferenced). Tests updated (`test_runner` no `_x_`/`_minus_`, plot6 absent; `test_use_case_sweep` plot6 dropped → 10 artifacts) | Requested this session — interaction features temporarily removed from model training | **No contract change** — the LOCKED 1.0/1.1 schema is untouched; `interaction_cols` still exists, just empty. Engine + UI + tests; 48 affected backend tests green, frontend `tsc -b` clean. **Caveat:** `interaction_cols`'s `_div_` marker overlaps the Section 7 `FeatureBuilder` ratio names (`a_div_b`), so a ratio column can still appear there when `feature_engineering.ratios` is on — pre-existing marker imprecision, unchanged here, harmless while the page is hidden. **Restore:** revert the `_engineer` force-disable + plot6 call, the three UI comment blocks, and the test edits |
| 44 | post-9 / v2.0 | Model persistence / MLflow was a deferred **v2.0** item (row #29): the `/explain` stub and the SHAP rewire both named "v2.0 model persistence / MLflow" as the enabler | **Pulled MLflow forward as Phase A** of the new Databricks-integration roadmap (`docs/databricks_integration.md` §6): an opt-in, lazy-imported, **report-only** MLflow logging + model-persistence layer (`classifyos/mlflow_logging.py`) — OFF by default, logging the config as params, each model's headline TEST metrics, the existing artifact files, and one flavor-native saved model per algorithm (`mlflow.xgboost`/`lightgbm`/`sklearn`, each unwrapped to its base estimator). Local store this phase (MLflow 3.x default = sqlite `mlflow.db` backend + `./mlruns` artifacts). Additive API bump **`1.8` → `1.9`** (`result.mlflow`). **Databricks Phases B/C (volume adapter, Model Serving) remain DEFERRED**; the near-term follow-up is the interim local Postgres phase (§6.5), NOT built here | Design-doc reprioritization (2026-07-08): stand up MLflow locally now (fixes the persistence/overwrite gaps + enables shareable weights) before Databricks, which is temporarily deferred | **A v2.0 item pulled into current work**, opt-in + OFF by default so a run with `mlflow.enabled=false` is byte-for-byte unchanged (asserted). The engine sets **NO tracking URI** — the store is swappable by `MLFLOW_TRACKING_URI` alone, so Interim-2a's Postgres backend is a pure config change (no code change). MLflow follows the shap/optuna opt-in/lazy-import discipline (not a web dep in the engine). [RISK] leakage: logging runs after training and reads nothing back into fit/transform. Reviewers: the `/explain` stub (row #29) is NOT yet wired to load persisted models — a later phase. `mlflow>=3.1,<4` added; hallucination-checked vs installed 3.14.0 |
| 43 | post-16 | Tuning had a **hard `600`s/model default timeout** (row 25, our own hardening over the prompt's `None`), and `tuning.search_space_overrides` — though in the locked contract since `1.0` — was **never exposed in the UI** (the frontend hardcoded `{}`) | **(a) Removed the default timeout cap everywhere** by owner request: `timeout_seconds` now defaults to `None` (no per-model wall-clock cap) in `config.py`, `api/models.py`, the frontend form, and the contract examples — **`n_trials` (still default 30) is now the SOLE bound** on a study. The Configure UI gains a **"No timeout" switch** (default on; unchecking restores a numeric cap field). **(b) Exposed `search_space_overrides` in the UI**: a new collapsible "Search space (advanced)" panel with one collapsible block per algorithm; numeric params get low/high override fields (blank = engine default), categoricals (`max_features`, SVM `kernel`) get choice checkboxes. New `frontend/src/lib/searchSpaces.ts` (read-only mirror of the engine `_space_*` bounds) + `TuningOverridesPanel.tsx` | Owner request (2026-06-26): remove the trial/timeout caps by default and let analysts edit the per-model search space from the browser | **This REVERSES row #25's safety hardening** — an enabled `models=[]` (tune-all) run now has no time ceiling, so it runs to completion of every algorithm's `n_trials` (incl. the slow calibrated-SVM); set `timeout_seconds` to re-impose a cap for large data / long `n_trials`. The `[RISK] runaway tuning` comment in `config.py` is **kept and rewritten** (not removed — governance rule) to document that `n_trials` is now the only bound. **No engine ML change and NO schema/version change** (field shapes unchanged; default-value change only — contract footer notes it). The override panel is purely additive — an untouched panel sends `{}` so a default-tuning run is unchanged; malformed overrides fall back to defaults via the existing per-model study isolation. Tests: `test_default_timeout_is_bounded` → `test_default_timeout_is_uncapped` + `test_default_n_trials_is_the_bound`; `buildPayload.test.ts` expects `timeout_seconds: null`. Engine/API tuning + API-run suites green (46); frontend build clean |
| 45 | post-A / Interim 2a | Databricks integration was the roadmap after Phase A; the design note (`docs/databricks_integration.md`) then **temporarily deferred Databricks** and inserted an **interim local phase** (§6.5) — explicitly flagged there as "a genuine, reversible deviation → log a plan_tweak.md entry when built" | **Built Interim 2a: MLflow's backend store moved to a local Postgres + a persistence read-path.** (a) **Store = Postgres, config-only:** point `MLFLOW_TRACKING_URI` at `postgresql://…` and set `_MLFLOW_SERVER_ARTIFACT_ROOT` to a local `file://` artifact folder — **the Phase-A engine logging code (`classifyos/mlflow_logging.py`) is UNCHANGED** (Phase A had left the store swappable by env alone, row #44). Pinned `psycopg2-binary` (MLflow's SQLAlchemy driver); env vars documented in `.env.example`. (b) **Read-path (additive API):** two NEW endpoints — `GET /api/v1/runs` (list past runs) and `GET /api/v1/runs/{run_id}` (reload one) — plus `/run` now persists its rendered envelope as a run artifact so reload is byte-identical; new `api/mlflow_read.py` (lazy `mlflow`, 503/404 mapping). (c) **Dashboard "Runs" view** lists past runs and reloads one into the existing result pages. **Contract bump `1.9 → 1.10`, additive — the `/run` envelope is byte-identical to `1.9`; only new endpoints were added** | Design-note reprioritization (2026-07-08): deliver real persistence locally (results survive a refresh + a server restart) and a database-backed foundation now, before Databricks — none of it wasted (MLflow-on-Postgres carries forward unchanged). Postgres over the sqlite default because it is SQL-queryable, multi-connection, and the closer analogue to a managed tracking server | **The version marker moved `1.9 → 1.10` even though the `/run` schema did not change** — recording the contract doc's advance (new endpoints) per the locked-contract rule; the frontend parser is version-tolerant so this is safe. Local-only: **no Databricks, no push.** Hallucination-checked vs installed **mlflow 3.14.0** — `postgresql` is an accepted backend-store scheme, `postgresql://` uses the psycopg2 dialect, and the artifact root for a DB store is the `_MLFLOW_SERVER_ARTIFACT_ROOT` env var (verified). **Verified live end-to-end** against a real local Postgres (PostgreSQL 17 via winget): two pipelines logged → both appear as rows in Postgres with params/metrics, artifacts (incl. saved models + the reload snapshot) on disk, and the read-path endpoints list + reload either after a refresh. Backend 381 tests green (+4); frontend 137 (+5); `tsc -b` + build clean. **Out of scope (still deferred):** Interim 2b (Postgres input source), Phase B (`DatabricksVolumeStorage`), Phase C (Model Registry/Serving), and wiring `/explain` to a persisted model |

| 46 | post-2a / Interim 2b | The interim local phase (`docs/databricks_integration.md` §6.5) — flagged there as "a genuine, reversible deviation → log a plan_tweak.md entry when built" — had 2a done (row #45); 2b (a Postgres INPUT source) was its remaining, still-deferred piece | **Built Interim 2b: an opt-in Postgres input source via materialize-to-file (Option B).** New engine config block `input_source` (`type: file` (default) \| `postgres`, `connection_env`, `table` \| `query`), validated by `build_config` (bad value → 422; a `table` must be a safe SQL identifier, the `input_file` destination must be `.parquet`/`.csv`). New pure engine module `classifyos/io/sql_source.py` (`materialize_source`): for a postgres source it runs the table/query ONCE via a lazy SQLAlchemy `create_engine` + `pandas.read_sql`, writes the result to `input_file` under DATA_DIR through `StorageAdapter.save_input`, and returns the key — a **no-op for the default file source**. Wired as a pre-step in `ModelRunner._load` BEFORE `data_loader`. API: additive `InputSourceConfig` + `RunConfig.input_source` forwarded to `build_config`; `/run` catches `InputSourceError` → the `status="error"` (400) envelope. **NO response-schema change and NO version bump** (a pure request-side dial). `SQLAlchemy` pinned in requirements.txt (psycopg2-binary already pinned from 2a); `CLASSIFYOS_PG_DSN` documented in `.env.example` | Design-note plan: cap the interim work at Phase A + 2a + 2b before Databricks; 2b gives a database-backed input now. Option B (materialize-to-file) over direct `pd.read_sql` (Option A) so ALL engine reads stay behind `StorageAdapter`, the leakage discipline (load → split → fit-on-train) stays literally untouched, and the query result is snapshotted for audit/repro | **`data_loader` and everything downstream are byte-for-byte unchanged** — the engine still reads a file; the DB step happens strictly before it. Opt-in + OFF by default (a `file`-source run is identical to before, asserted). **No hardcoded credential** — `connection_env` names a server-side env var (`.env`, machine-local) holding the DSN; the request never carries a credential. Local-only: **no Databricks, no push.** Hallucination-checked vs installed **SQLAlchemy 2.0.51 / psycopg2 2.9.12 / pandas 2.3.3** (`create_engine`, `text`, `read_sql` verified). **Verified live** against a real local Postgres (`classifyos_data` DB, `policy_lapse` table, PostgreSQL 17): the materialized snapshot holds the identical 3000-row set as the CSV, and a `SELECT * … ORDER BY policy_id` run reproduces the direct-CSV metrics **bit-for-bit** (0.00e0 diff). **Row-order caveat (documented):** a SQL table is a *set* — a bare `table`/`SELECT *` returns rows in an unspecified order, so the seeded train/test split may differ slightly from the CSV; add an `ORDER BY` for a byte-reproducible snapshot. CLI flags for the source were left out of scope (engine+API first; the runner-level support is usable programmatically). Tests: new `test_sql_source.py` (+18: config validation, sqlite-DSN materialize round-trips, failure modes, end-to-end postgres==csv equivalence) + 3 API tests (file default unchanged; bad source → 422; unset-DSN → error envelope). **Still deferred:** Phase B (`DatabricksVolumeStorage`), Phase C (Model Registry/Serving), `/explain`→persisted-model wiring, dashboard UI to pick a table/query |

| 47 | §6.6 Step 6 | Row #28 recorded that `POST /api/v1/run` is **synchronous** and a submit→poll→fetch background path is **"deferred to v1.5"**; the Step 6 task said to **"replace"** `/run` with the async Databricks-Jobs pattern | **Built the async background-job path — but env-GATED and ADDITIVE, not a hard replace.** `CLASSIFYOS_EXECUTION_BACKEND` (default `local`) selects the behaviour: `local` keeps the exact synchronous `/run` (byte-identical; all ~461 backend + 159 frontend tests green, no local cluster needed), `databricks` makes the same `/run` submit a Databricks Job (`jobs/runs/submit`) and return `{job_id, run_id}`, polled via new `GET /run/{job_id}/status` and fetched via `GET /run/{job_id}/results`. New: `api/databricks.py` (REST client + `RunState`→status mapping + UC proxies), `api/jobs_store.py` (a `classifyos_jobs` table via SQLAlchemy Core, reusing the MLflow Postgres — restart-safe), `api/result_builder.py` (the `/run` reshaper extracted verbatim from `routes/run.py` so the route AND the Job notebook share ONE reshaper), `api/routes/jobs.py` + `api/routes/databricks.py`. Contract bump **`1.10 → 1.11`** (additive); `/health` gains `execution_backend`; the request `input_source` gains optional `catalog`/`schema`/`limit` (Delta). `httpx` promoted to a runtime API dep. Frontend: the store polls (every 5s) + Overview spinner + a Databricks (Unity Catalog) data-source tab | The literal "replace" collides with CLAUDE.md's hard rules (additive-only, local byte-identical, CI must not need Databricks) and would break the whole local/test flow (there is no local cluster). **Owner-confirmed** the env-gated interpretation (AskUserQuestion) — the async path is the *deployed Azure* behaviour, gated on exactly like every other optional integration (mlflow/delta/postgres/storage) | **Supersedes row #28's "deferred to v1.5"** — the background-job path now exists, but OFF by default so nothing local changes. The user's PAT (`X-Databricks-Token`) is forwarded to the Job for UC data access and **never persisted**; the service token is used only for the Jobs API. **All Databricks REST calls mocked in CI (`httpx.MockTransport`); no test contacts a live workspace** (`test_api_jobs.py` + `test_api_databricks.py`); jobs store = temp sqlite. The `1.10 → 1.11` bump forced updating `schema_version` assertions in `test_api_run`/`test_api_runs`/`test_use_case_sweep` + the additive `execution_backend` field in `test_api_health`. Hallucination-checked vs Microsoft Learn / Azure Databricks (Jobs 2.1 submit/get, `state.life_cycle_state`/`result_state`, UC `catalogs`/`schemas`/`tables` params); the API-reference site is a JS SPA so the client is written defensively (tolerates `SUCCESS`/`SUCCEEDED`, missing fields). **NOT run on a real cluster** (Job notebook `notebooks/classifyos_job_runner.py` written, cluster run pending). Engine sections UNTOUCHED |
| 48 | §6.6 Step 6 | The Step 6 UI brief said the frontend picks "Databricks as a data source" via the catalog/schema/table picker | **The UC browser proxies (Part C) return NAMES ONLY (no columns), so a Unity Catalog table cannot be column-profiled in the UI yet.** The Databricks data-source tab therefore browses catalog→schema→table and sets a `delta` `input_source`, but the analyst enters the **target + feature columns manually** and the run uses **engine defaults** (there is no profile to drive the rich Configure page). A UC-table profiling endpoint (analogous to `/upload` / `/input-sources/select`) is a **documented follow-up** | The given Part C scope is the three name-list proxies (`unity-catalog/{catalogs,schemas,tables}`); profiling a UC table needs a separate read (columns/dtypes/sample), which was not in scope and can't be exercised without a cluster | **A knowingly-bounded UI:** the polling run-flow (the primary deliverable) works for any submitted run; the Databricks picker is functional (sets `input_source`+`input_file`, runs) but not as rich as the file/postgres Configure path. No contract impact. Recorded so a reviewer knows the UC data-source UX is intentionally minimal in this cut. **✅ RESOLVED 2026-07-14:** built the follow-up — `GET /api/v1/databricks/table-profile` fetches the UC table's schema (get-a-table) and returns the **same `InspectProfile` shape as `/upload`** (+ a `delta` `input_source`), so the Databricks picker now drives the same column picker as a file upload via the existing `applyUpload` plumbing — no manual column entry. Additive, upload/profile-side (no `schema_version` bump); Databricks REST mocked in tests. This closes the bounded-UX note above; the Databricks Configure path is now as rich as file/postgres |

---

## How to read this register

- A row exists wherever reality differs from the signed scope, or fills a gap the scope
  left open. Most are deliberate, recorded decisions (see PROJECT_STATE.md decisions log).
- The two rows a reviewer should weigh most at sign-off: **#4** (pipeline-order correction
  vs. the scope diagram) and **#5** (synthetic data — metrics not yet representative).
- See `backend_short_desc.md` for plain-language phase summaries, CLAUDE.md for the hard rules.

## Housekeeping note (2026-06-16)

Repo reorg session (prompt: `prompts/tooling/reorg.md`): organised `prompts/` into
subfolders, removed the doc-update Stop hook (`scripts/check_docs_updated.py` +
`.claude/settings.json` entry), renamed `short_desc.md` → `backend_short_desc.md`, and
backfilled/verified the Phase 7 summary. **No scope deviation (repo housekeeping only)** —
no engine code touched, no behaviour changed.
